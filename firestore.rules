rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEST MODE: Allow all reads and writes for development
    // TODO: Replace with production rules before going live
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
